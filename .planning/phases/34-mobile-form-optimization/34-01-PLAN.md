---
phase: 34-mobile-form-optimization
plan: 01
type: execute
---

<objective>
Fix form layouts to stack properly on mobile by adding responsive breakpoints to all grid-based form field groups.

Purpose: Ensure all multi-column form fields stack to single column on mobile (<640px) for proper usability on small screens.
Output: All forms use consistent `grid-cols-1 sm:grid-cols-2` pattern matching existing responsive forms.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Established pattern (from profileForm.tsx and dealForm.tsx):**
```tsx
<div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
```

**Files requiring fixes:**
- signUpForm.tsx - 1 location (First/Last name)
- businessProfileForm.tsx - 3 locations (City/State, Zip/Area, Hours day/time)
- createBusinessForm.tsx - 2 locations (City/State, Zip/Area)
- dealForm.tsx - 1 location (nested Min/Max units grid)
- mockPaymentForm.tsx - 2 locations (Expiry/CVC, State/ZIP)

**Already responsive (no changes needed):**
- profileForm.tsx - Already uses `grid-cols-1 sm:grid-cols-2`
- dealForm.tsx main grids - Already responsive
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix signUpForm responsive grid</name>
  <files>src/components/features/signUpForm.tsx</files>
  <action>
  Change the First/Last name grid from:
  `grid grid-cols-2 gap-4`
  to:
  `grid grid-cols-1 sm:grid-cols-2 gap-4`

  This affects the name fields around line 86.
  </action>
  <verify>grep -n "grid-cols-1 sm:grid-cols-2" src/components/features/signUpForm.tsx shows the fix</verify>
  <done>signUpForm uses responsive grid for name fields</done>
</task>

<task type="auto">
  <name>Task 2: Fix businessProfileForm responsive grids</name>
  <files>src/components/features/businessProfileForm.tsx</files>
  <action>
  Find all instances of `grid grid-cols-2 gap-4` and change to `grid grid-cols-1 sm:grid-cols-2 gap-4`.

  There are 3 locations:
  1. City/State fields (around line 345)
  2. Zip/Location area fields (around line 399)
  3. Any other two-column grids in the form

  Use replace_all since pattern is consistent.
  </action>
  <verify>grep -c "grid-cols-1 sm:grid-cols-2" src/components/features/businessProfileForm.tsx shows 3+ matches</verify>
  <done>businessProfileForm uses responsive grids for all two-column field groups</done>
</task>

<task type="auto">
  <name>Task 3: Fix createBusinessForm and remaining forms</name>
  <files>src/components/features/createBusinessForm.tsx, src/components/features/dealManagement/dealForm.tsx, src/components/features/mockPaymentForm.tsx</files>
  <action>
  Apply the same fix to remaining forms:

  1. createBusinessForm.tsx - 2 locations (City/State, Zip/Area)
  2. dealForm.tsx - 1 location (nested Min/Max units grid around line 381)
  3. mockPaymentForm.tsx - 2 locations (Expiry/CVC, State/ZIP)

  Change: `grid grid-cols-2 gap-4`
  To: `grid grid-cols-1 sm:grid-cols-2 gap-4`
  </action>
  <verify>
  grep -c "grid-cols-1 sm:grid-cols-2" src/components/features/createBusinessForm.tsx && \
  grep -c "grid-cols-1 sm:grid-cols-2" src/components/features/dealManagement/dealForm.tsx && \
  grep -c "grid-cols-1 sm:grid-cols-2" src/components/features/mockPaymentForm.tsx
  </verify>
  <done>All forms use consistent responsive grid pattern</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] No remaining `grid grid-cols-2 gap-4` without `sm:` breakpoint in form files
- [ ] All form files use consistent `grid-cols-1 sm:grid-cols-2 gap-4` pattern
</verification>

<success_criteria>

- All 5 form files updated with responsive breakpoints
- Consistent pattern across all multi-column form fields
- Build passes with no errors
- Forms stack to single column on mobile (<640px)
</success_criteria>

<output>
After completion, create `.planning/phases/34-mobile-form-optimization/34-01-SUMMARY.md`
</output>
